// const Joi = require('joi');

// function validateOverview(metagame) {
//     const schema = {
//       name: Joi.string().min(1).max(255).required(),
//       numberOfTotalDecks: Joi.number().min(1).max(50).required(),
//       percentageOfMetagame: Joi.number().min(1).max(255).required(),
//       archetype: Joi.string().min(1).max(50).required(),
//     };
  
//     return Joi.validate(metagame, schema);
//   }

//   exports.validateMetagame = validateMetagame;